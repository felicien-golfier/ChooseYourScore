✅ TÂCHE 1 — Fusion des types de paires (séquence universelle)
   - migrateAllPairs() : migration auto choice/arrow → sequence au démarrage
   - HTML supprimé : #pair-container, #arrow-container, #pair-type, #arrow-config, #choice-config
   - CSS supprimé : .pair-container, .arrow-container, .arrow-card, .arrow-key-btn, .modal-preview, .items-edit-grid, etc.
   - JS supprimé : handleClick, handleArrowResponse, fillModalSide, readModalSide, playPairAudios, etc.
   - renderPair() / openPairModal() / btn-modal-save unifiés (séquence uniquement)
   - #seq-item-modal créé (IDs sim-*) avec cartes visuelles seq-item-card
   - Type de question 'direction' : showDirectionButtons + handleDirectionResponse
   - allowRetry par question (défaut true)

✅ TÂCHE 2 — Zéro code legacy
   - Règle appliquée : toute suppression de système supprime aussitôt son HTML/CSS/JS
   - Seules exceptions tolérées : fonctions migrateXxx() pour la migration localStorage

✅ TÂCHE 3 — Simplification / DRY
   - downloadBlob() helper (3 usages)
   - getFocusedOrLastChoice() helper (2 usages)
   - getPairQuestions() helper (2 usages)
   - shouldShowItems unifie keepVisible + showOnRetry
   - exportSessionsJson / importSessionsJson partagés patients/résultats
   - CSV paires export/import unifié séquence

